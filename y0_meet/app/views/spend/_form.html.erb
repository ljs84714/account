<%= form_for([@account,@spend]) do |f| %>
  <% if @spend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spend.errors.count, "error") %> prohibited this spend from being saved:</h2>

      <ul>
      <% @spend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :info %><br>
    <%= f.text_field :info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
